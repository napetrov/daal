pull_request_rules:
  - name: backport patches to stable branch
    conditions:
      - base=master
      - label=backport-to-releases
    actions:
      backport:
        branches:
          - releases
